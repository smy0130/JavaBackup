package goodsandrawmaterial;

public class Main {
    public static void main(String[] args) {
        // Goods 클래스 객체 생성 및 메서드 호출
        Goods goods = new Goods();
        goods.goods(); // 수정된 메서드 이름 goods()를 호출합니다.

        // Rawmaterial 클래스 객체 생성 및 메서드 호출
        Rawmaterial rawmaterial = new Rawmaterial();
        rawmaterial.Rawmaterial(); // 수정된 메서드 이름 Rawmaterial()을 호출합니다.
    }
}